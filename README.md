# Fitness Web

A web client for "Fitness routine manager" project

## Development
Install dependencies
- [nodejs](https://nodejs.org/en/download/current) (make sure to install it along with `npm`)

Install project-specific dependencies:
```bash
npm install
```

Run the dev server
```bash
npm run dev
```

Build for production
```bash
npm run build
```
